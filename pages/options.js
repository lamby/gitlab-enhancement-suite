var GitLabEnhancementSuiteOptions = [
    {
      "name": "merge_request_allow_collaboration"
    , "title": "Allow MR edits by default"
    , "description": "Toggles the 'Allow commits from members who can merge to the target branch' checkbox on new merge requests by default."
    , "image": false
    , "default": false
  } ,
    {
      "name": "remove_source_branches"
    , "title": "Remove source branches by default"
    , "description": "Toggles the 'Remove source branch when merge request is accepted' checkbox on new merge requests by default."
    , "image": false
    , "default": false
  }
]
